# Copy this file to .env and fill in values.
# Used by docker-compose.prod.eleven.yml and update.sh.
# Common values below were filled from .vscode/.env; set ACR_REGISTRY, POSTGRES_PASSWORD, WEB_DOMAIN for deployment.

# --- Azure / Docker ---
# Azure Container Registry URL (e.g. myregistry.azurecr.io). Required for pulling images.
ACR_REGISTRY=
# Image tag to deploy (e.g. latest, nightly-20250128).
IMAGE_TAG=latest
# Optional: code-interpreter image tag if using that service.
# CODE_INTERPRETER_IMAGE_TAG=latest

# --- Nginx / Let's Encrypt ---
# For HTTPS: set DOMAIN and optionally EMAIL in .env.nginx (see README).
# Nginx proxy timeouts in seconds (optional; defaults 300).
# NGINX_PROXY_CONNECT_TIMEOUT=300
# NGINX_PROXY_SEND_TIMEOUT=300
# NGINX_PROXY_READ_TIMEOUT=300

# --- Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=onyx
# Optional read-only DB user.
# DB_READONLY_USER=db_readonly_user
# DB_READONLY_PASSWORD=

# --- Application / Auth ---
# e.g. https://your-domain.com
WEB_DOMAIN=
# Auth: disabled | basic | google_oauth | oidc | saml (from .vscode/.env)
AUTH_TYPE=disabled
# Required for many auth types
# SECRET=
# GOOGLE_OAUTH_CLIENT_ID= / GOOGLE_OAUTH_CLIENT_SECRET= for Google OAuth
# OPENID_CONFIG_URL= for OIDC

SESSION_EXPIRE_TIME_SECONDS=604800
# VALID_EMAIL_DOMAINS= for restricting sign-in by domain

# --- MinIO / S3 ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_FILE_STORE_BUCKET_NAME=onyx-file-store-bucket

# Show extra/uncommon connectors (from .vscode/.env)
SHOW_EXTRA_CONNECTORS=True

# --- Optional: Azure Key Vault ---
# If you store certs or secrets in Key Vault, set the vault name (used by init-letsencrypt if integrated).
# AZURE_KEY_VAULT_NAME=

# --- Optional: Model server / workers ---
# DISABLE_MODEL_SERVER=True to skip model server containers
# USE_LIGHTWEIGHT_BACKGROUND_WORKER=true
LOG_LEVEL=info
